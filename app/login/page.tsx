'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function Login(){const [id,setId]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({studentId:id,password})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Login failed');return}router.push('/dashboard')}return <main><section className="container pageHero" style={{textAlign:'center'}}><span className="eyebrow">Secure student area</span><h1>Welcome back.</h1><p style={{margin:'auto'}}>Use the Student ID and password supplied by the teacher. New students can register through WhatsApp.</p></section><section className="container loginBox"><form className="form" onSubmit={submit}><div className="field"><label>Student ID</label><input value={id} onChange={e=>setId(e.target.value)} placeholder="e.g. SCI26-001" required /></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" required /></div>{error&&<div className="notice">{error}</div>}<button className="btn primary" style={{width:'100%'}} disabled={loading}>{loading?'Checking…':'Login →'}</button></form></section></main>}
